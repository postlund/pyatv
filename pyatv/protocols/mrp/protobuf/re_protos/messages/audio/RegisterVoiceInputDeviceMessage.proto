syntax = "proto2";

import "protocol/ProtocolMessage.proto";
import "types/audio/VoiceInputDeviceDescriptorMessage.proto";

extend ProtocolMessage {
  optional RegisterVoiceInputDeviceMessage registerVoiceInputDeviceMessage = 33;
}

message RegisterVoiceInputDeviceMessage {
  optional VoiceInputDeviceDescriptor deviceDescriptor = 1; // NOTE(RE): field name is "descriptor" in RE proto
}
