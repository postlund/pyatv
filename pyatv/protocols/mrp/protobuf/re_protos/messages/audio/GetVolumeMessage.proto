syntax = "proto2";

import "protocol/ProtocolMessage.proto";

extend ProtocolMessage {
  optional GetVolumeMessage getVolumeMessage = 53;
}

message GetVolumeMessage {
  optional string outputDeviceUID = 1;
}
